<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.carrot.market.mapper.LoginMapper">

    <insert id="insertCustomer">
        <selectKey keyProperty="customerId" order="BEFORE" resultType="long">
            SELECT SEQ_CUSTOMER_ID.NEXTVAL FROM DUAL
        </selectKey>

         INSERT INTO CUSTOMER (
            CUSTOMER_ID, CUSTOMER_NO, USER_ID, PASSWORD, NAME,
            ZIPCODE, ADDRESS, ADDRESS_DTL
         ) VALUES (
            #{customerId}, #{customerNo}, #{userId}, #{password}, #{name}, 
            #{zipcode}, #{address}, #{addressDtl}
         )

    </insert>



</mapper>